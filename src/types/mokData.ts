import {RecordStatus} from "@/types/types";

export const recordStatusMap: Map<string, RecordStatus> = new Map([
    ['20250403', 'normal'],
    ['20250408', 'abnormal'],
    ['20250410', 'active'],
]);